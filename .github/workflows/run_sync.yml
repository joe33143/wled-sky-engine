%%MAGIT_PARSER_PROTECT%%```yamlname: Sync Varanasi Sky via Adafruit IOon:schedule:- cron: '*/10 * * * *'workflow_dispatch:env:FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true # Updates runner execution environment to current standardsjobs:run-script:runs-on: ubuntu-lateststeps:- name: Checkout Codeuses: actions/checkout@v4- name: Setup Pythonuses: actions/setup-python@v5with:python-version: '3.10'cache: 'pip'- name: Install Dependenciesrun: |pip install -r requirements.txt- name: Run Atmospheric Engineenv:OPENWEATHER_API_KEY: ${{ secrets.OPENWEATHER_API_KEY }}AIO_USERNAME: ${{ secrets.AIO_USERNAME }}AIO_KEY: ${{ secrets.AIO_KEY }}run: python sky_processor.py%%MAGIT_PARSER_PROTECT%%```
